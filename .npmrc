# node_modules plano (como npm), por consistencia con el backend y para evitar
# problemas de resolución con paquetes que asumen hoisting.
# pnpm sigue bloqueando los scripts postinstall (ver pnpm-workspace.yaml).
node-linker=hoisted
